
이 문서 전체를 프로젝트 루트 디렉토리에 **`claude.md`** 또는 **`instructions.md`** 파일로 저장하시면, AI 에이전트가 지시 없이도 이 구조를 파악해 뼈대부터 완성형 코드까지 한 번에 빌드하기 시작할 것입니다.

---

# 📝 [Project Artifact] claude.md

```markdown
# 시스템 지시서: 저메추 룰렛 앱 (Dinner Menu Roulette)

> **AI 가이드:** 이 문서는 '저메추 룰렛 앱' 개발을 위한 절대적인 가이드라인(Single Source of Truth)입니다. 이 파일을 읽은 후, 별도의 수동 지시가 없더라도 정의된 파일 구조(`index.html`, `index.css`, `index.js`)에 맞추어 즉시 제품 수준의 프로토타입 코드를 작성하십시오. 외부 라이브러리(jQuery, React 등) 없이 순수 Vanilla JS와 Canvas API로만 구현해야 합니다.

---

## 📁 1. 권장 파일 구조

모든 소스코드는 모듈성과 유지보수성을 극대화하기 위해 아래와 같이 엄격히 3개 파일로 분리하여 구성합니다.
- `index.html`: 앱의 시각적 뼈대 및 구조 정의 (마크업에 인라인 스타일/스크립트 금지)
- `index.css`: 모던 다크 테마 변수 및 레이아웃 시스템 정의 (Flexbox, Grid 최적화)
- `index.js`: Canvas 룰렛 렌더링, 물리 회전 애니메이션, 상태 관리(CRUD 및 영속성) 로직 전체

---

## 🛠 2. 파일별 핵심 구현 스펙 및 소스코드 기획

### 2.1 index.html (구조 정의)
- **시맨틱 마크업**: 메인 레이아웃은 크게 좌측 룰렛 섹션(`<main class="roulette-container">`)과 우측 제어판 섹션(`<aside class="control-panel">`)의 2컬럼 레이아웃으로 구성합니다.
- **룰렛 플레이어**: 룰렛이 그려질 고정 해상도의 `<canvas id="rouletteCanvas" width="450" height="450"></canvas>` 요소와 그 중앙에 절대 좌표(absolute)로 겹쳐질 `SPIN` 버튼을 배치합니다. canvas 상단 정중앙에는 아래를 가리키는 역삼각형 모양의 화살표 인디케이터 역할을 할 DOM 요소를 구성합니다.
- **컨트롤 패널**:
  - **프리셋 영역**: 4개의 버튼(직장인 인기, 자취생 초간단, 건강 & 다이어트, 침샘자극 야식)을 격자 또는 균등 배치 형태로 구성합니다.
  - **후보지 목록 영역**: 현재 활성 개수를 보여주는 타이틀, 메뉴 추가를 위한 인라인 폼(`<form id="menuForm">`), 그리고 추가된 메뉴들이 뱃지 형태로 뿌려질 컨테이너(`<div id="badgeContainer">`)를 배치합니다.
  - **하단 유틸리티**: `전체삭제`와 `기본메뉴` 버튼이 1:1 비율로 정렬된 액션 바를 구성합니다.

### 2.2 index.css (디자인 시스템 & 테마 변수)
- **CSS 변수 기반 모던 다크 테마**: 이미지 피드백을 반영하여 딥 차콜과 다크 그레이 계열의 고급스러운 야간 인터페이스를 기본으로 설계합니다.
  ```css
  :root {
    --bg-main: linear-gradient(135deg, #1f1c2c 0%, #928dab 100%); /* 배경 그라디언트 */
    --bg-card: #2a2a35;             /* 메인 섹션 및 카드 배경 */
    --bg-input: #1e1e24;            /* 입력창 내부 배경 */
    --text-primary: #ffffff;        /* 기본 화이트 텍스트 */
    --text-muted: #a0a0b0;          /* 보조 및 설명용 회색 텍스트 */
    --accent-orange: #ff7a00;       /* SPIN 버튼 및 주요 포인트 컬러 */
    --accent-yellow: #ffcc00;       /* 추가 버튼 및 활성 상태 포인트 컬러 */
    
    /* 룰렛 섹션별 무지개톤 파스텔/비비드 컬러 에레이 */
    --wheel-color-1: #ff5e62; --wheel-color-2: #ff9966;
    --wheel-color-3: #ffde17; --wheel-color-4: #56ab2f;
    --wheel-color-5: #a8ff78; --wheel-color-6: #00c6ff;
    --wheel-color-7: #0072ff; --wheel-color-8: #fbc7d4;
    --wheel-color-9: #9796f0; --wheel-color-10: #b06ab3;
  }

```

* **레이아웃 매커니즘**:
* 화면 전체를 중앙 정렬 마진 구조로 감싸고, 데스크탑 기준 `display: flex; gap: 32px;`를 사용하여 균형 잡힌 2컬럼 레이아웃을 생성합니다.
* 모든 버튼에는 `transition: all 0.25s ease;`를 부여하고 `:hover` 및 `:active` 시 스케일 변형(`transform: scale(0.98)`)과 그림자 효과를 주어 쫀득한 물리적 클릭감을 구현합니다.
* 메뉴 후보지 뱃지들은 `display: inline-flex; align-items: center;` 구조로 설계하며, 체크박스가 해제(비활성화)되면 투명도를 `opacity: 0.4;`로 낮추고 룰렛 렌더링 대상에서 즉각 제외하는 시각적 피드백을 제공합니다.


* menu roulette.jpg을 참고해서 UI를 구성해줘.


### 2.3 index.js (비즈니스 로직 & 룰렛 엔진)

* **상태 정의 (State)**:
```javascript
let menuItems = []; // { id, text, isEnabled } 구조의 객체 배열
let currentAngle = 0; // 룰렛의 현재 회전 각도 (라디안 또는 도)
let isSpinning = false; // 현재 회전 중인지 여부 플래그

```


* **기본 메뉴 12종 세트**: 초기 실행 시 혹은 '기본메뉴' 클릭 시 로드할 고정 데이터셋입니다.
`["제육볶음", "돈까스", "초밥", "치킨", "삼겹살", "짜장면", "부대찌개", "마라탕", "파스타", "햄버거", "떡볶이", "연어샐러드"]`
* **Canvas 룰렛 드로잉 핵심 알고리즘**:
* `menuItems` 중 `isEnabled: true`인 요소만 필터링하여 드로잉 타겟 배열을 만듭니다.
* 활성화된 메뉴 개수가 $N$개일 때, 각 부채꼴의 중심각은 $\theta = 360^\circ / N$이 됩니다.
* `context.arc()`와 `context.rotate()`를 활용해 중심에서 방사형으로 부채꼴과 텍스트를 정밀 렌더링합니다. 텍스트가 원판 밖으로 나가지 않도록 캔버스 반지름의 약 60% 지점에 위치시킵니다.


* **물리 이징 회전 애니메이션**:
* 룰렛의 회전 속도감을 살리기 위해 단순 선형 회전이 아닌 `Cubic Ease-Out` 공식을 적용합니다. 초기 구동 시 최소 5바퀴 이상 회전하도록 기본 오프셋 각도를 높게 설정한 뒤, 브라우저 주사율에 동기화되는 `requestAnimationFrame` 루프로 자연스럽게 감속하며 멈추게 만듭니다.
* 회전이 완전히 정지하면 상단 화살표 인디케이터($270^\circ$ 혹은 $-\pi/2$ 지점)가 가리키는 부채꼴의 인덱스를 계산하여 최종 당첨 메뉴를 모달창(`alert` 대용 커스텀 레이어 권장)으로 띄웁니다.


* **영속성 및 이벤트 연동**:
* 데이터 배열(`menuItems`)에 변경이 일어날 때마다(추가, 개별 삭제, 토글, 프리셋 로드, 전체 삭제 등) 자동으로 `localStorage.setItem('dinner_todos', JSON.stringify(menuItems))`를 호출합니다.
* 애플리케이션 초기화 프로세스 시 로컬 스토리지에 데이터가 존재하면 이를 읽어와 화면 리스트와 룰렛 Canvas를 즉시 렌더링(`render()`)합니다.



---

## 🎯 3. 최종 수용 기준 (Acceptance Criteria)

AI 에이전트는 코드를 모두 작성한 후 아래의 체크리스트가 전부 통과하는지 검증해야 합니다.

* [ ] **유효성 검사**: 빈 문자열이나 스페이스바 공백만 입력했을 경우 메뉴 추가가 차단되는가?
* [ ] **동적 반영**: 우측 리스트에서 특정 메뉴의 체크박스를 해제하면 좌측 룰렛 원판에서 해당 메뉴 구획이 실시간으로 사라지고 나머지 메뉴들이 균등 각도로 재정렬되는가?
* [ ] **애니메이션 안정성**: 룰렛이 한창 돌아가고 있는 도중(isSpinning === true)에 SPIN 버튼을 다시 누르거나 메뉴를 추가하는 등의 비정상 조작이 원천 차단되는가?
* [ ] **영속성 유지**: 임의의 메뉴 구성 상태에서 브라우저를 새로고침(F5)해도 구성했던 후보지 리스트와 체크 상태가 온전하게 유지되는가?
* [ ] **번개 추천**: 하단의 번개 추천 버튼을 누르면 회전 효과 없이 0.1초 이내에 활성 후보 중 하나가 무작위로 완벽히 추출되어 팝업으로 표시되는가?

```

---

### 💡 파일 생성 후 실행 방법 명세
1. 위 코드 블록 안의 마크업 텍스트 전체를 복사하여 프로젝트 최상위에 `claude.md`로 저장합니다.
2. Claude Code 터미널 또는 Cursor AI 창을 열고 아래와 같이 프롬프트를 입력하면 자동 빌드가 시작됩니다.

> **작업 요청 프롬프트 예시:**
> *"프로젝트 루트에 있는 `claude.md` 파일의 요구사항과 아키텍처를 확인하고, 기술 스택 및 비기능적 요구사항에 완벽히 부합하는 `index.html`, `index.css`, `index.js` 소스코드를 작성해줘."*

```